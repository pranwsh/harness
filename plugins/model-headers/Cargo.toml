[package]
name = "harness-model-headers"
version = "0.1.0"
edition = "2024"

[dependencies]
harness-contracts = { path = "../../contracts" }
harness-core = { path = "../../core" }
harness-config = { path = "../config" }

[dev-dependencies]
tokio.workspace = true
